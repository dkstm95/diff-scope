{
  "name": "diff-scope",
  "version": "0.1.0-alpha",
  "private": true,
  "type": "module",
  "description": "Understand completed local code changes with explanations, quizzes, and interactive microworlds.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check:release": "node tools/check-release.mjs",
    "check": "npm run check:release && npm test"
  }
}
