{
  "name": "hope",
  "version": "0.4.0-alpha",
  "private": true,
  "type": "module",
  "description": "A small AI work harness with pull request understanding and safe cleanup.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check:release": "node tools/check-release.mjs",
    "check": "npm run check:release && npm test"
  }
}
