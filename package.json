{
  "name": "hope",
  "version": "0.2.0-alpha",
  "private": true,
  "type": "module",
  "description": "Align intent before coding and understand the resulting change before merge.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check:release": "node tools/check-release.mjs",
    "check": "npm run check:release && npm test"
  }
}
